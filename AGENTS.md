# AGENTS.md

Project EVE Demo 与 Codex 的项目级协作协议。根文件只保存全局硬约束；具体模块规则优先放在对应目录的 `AGENTS.md`。

## 0. 项目目标

- 本项目是用于求职作品演示的 Unity 3D 战斗系统 Demo。
- 核心目标是制作主角与 Raven 风格 Boss 的动作战斗原型，重点展示玩家控制、状态机、近战攻击、Guard / Evade / Skill、Hitbox / Hurtbox、Boss AI、战斗镜头和反馈手感。
- 非核心目标默认不做：开放世界、背包、任务、对话、商店、复杂非战斗 UI 等。
- 环境：Unity 6000.3.15f1、URP、Unity Test Framework。

## 1. 禁止读取与例外

默认禁止读取或修改以下非项目开发上下文，避免无效 token 与误操作：

- `.vscode/`
- `doc/.obsidian/`
- `doc/player_state_design_core_docs/`
- `doc/*.base`
- `otherSource/`
- `*.slnx`
- `*.lscache`
- `*.csproj`
- `build/`、`Build/`
- `Library/`、`library/`
- `obj/`、`Obj/`
- `Logs/`、`logs/`
- `UserSettings/`
- `temp/`、`Temp/`

例外：

- `ProjectSettings/` 默认禁读；只有用户明确授权或任务必须修改 Project Settings 时才可读取，并在交付中说明。
- `Packages/` 默认不通读；确认包版本优先用 Unity MCP、Package Manager 或官方文档，只在必要时读取具体文件。
- 用户明确指定的文件优先于禁读规则，但仍避免无关扩展阅读。

## 2. 每轮启动流程

接收任务后必须先执行：

1. 使用 `sequential-thinking` 做任务理解、风险识别和执行策略分析。
2. 读取根 `AGENTS.md`。
3. 读取 `doc/当前上下文摘要.md`、`doc/索引/模块索引.md`、`doc/索引/任务关键词路由.md`。
4. 按任务关键词读取对应模块长期记忆、需求变更和需求文档。
5. 修改或读取具体功能目录时，如果该目录或父级功能目录存在 `AGENTS.md`，必须读取并遵守其中的局部规则。
6. 只有追溯历史原因、旧规则或完整审计时，才用 `rg` 定位并读取 `doc/项目长期记忆.md` 或 `doc/需求变更记录.md` 的相关段落；禁止默认全文读取。
7. 涉及 Unity 开发时，优先用 Unity MCP 检查项目、脚本、场景、控制台和测试结果。

上下文规则：禁止无目的通读全部文档；优先用 `rg`、Unity MCP、目录 README、文件头和必要函数定位事实；简单任务只读必要上下文；复杂任务先明确目标、影响范围、验收标准和验证方式。

## 3. 文档记忆与需求变更

必须维护项目记忆体系：

- 启动摘要：`doc/当前上下文摘要.md`。
- 索引入口：`doc/索引/模块索引.md`、`doc/索引/需求变更索引.md`、`doc/索引/任务关键词路由.md`。
- 模块记忆：`doc/长期记忆/*.md`。
- 模块需求变更：`doc/需求变更/*.md`。
- 历史追溯源：`doc/项目长期记忆.md`、`doc/需求变更记录.md`。

维护规则：

- 需求、状态机、输入、Timeline、伤害判定、Boss AI 等语义变更不能只改代码，必须同步更新相关需求文档和变更记录。
- 不涉及代码、资产的改动，只是方案文档层面的构建，不要把它当做需求变更；只有真正影响项目代码、资产、运行时语义、目录职责或验收标准时，才更新需求变更以及长期记忆。
- 阶段性交付后更新当前上下文摘要和相关模块长期记忆，记录下一步入口、已知风险和验证结果。
- 修改代码文件职责时同步更新文件头说明；修改目录模块边界时同步更新目录 README。
- 新对话必须能通过根 `AGENTS.md`、当前上下文摘要、索引文件和模块文档恢复项目状态。

## 4. Unity 工作流

- Unity 相关任务优先使用 Unity MCP；脚本变更后刷新 Unity 并读取 Console error。
- 避免常规脚本变更使用 `refresh_unity(wait_for_ready=true)`；当前 Editor readiness 资源可能不可用，容易固定等待 60 秒超时。默认使用 `refresh_unity(..., wait_for_ready=false)`，若返回 compiling，短等 3-5 秒后重试 `read_console` 或相关测试；只有确认 readiness 资源可用且确实需要完整等待时才允许长等。
- 创建、读取、校验 C# 脚本优先使用 Unity MCP 或本地检索；新增 `.cs` 后必须触发 Unity 资产刷新，并确认对应 `.meta` 已生成，再短等 3-5 秒读取 Console 或运行测试，避免 Unity 尚未导入新类型造成假失败。
- 修改 GameObject、场景、Prefab、材质、相机、动画、UI、物理、测试时优先使用对应 Unity MCP 工具。
- Unity API、Input System、URP、Cinemachine、包版本或平台限制不确定时，优先用 Unity MCP 反射、官方文档或官方网页确认。
- 本地项目事实优先用 `rg` 和本地文件，不为项目内代码关系联网。

## 5. 计划、阶段与交接

- 拒绝直接实现过长且容易发散的目标：遇到跨多个系统、验收边界不清、预计需要长时间连续写作或编码的目标，必须先拆成可交付、可验证的小工作单；用户未确认单步范围前不得继续自动推进。
- 复杂功能必须分阶段推进；每阶段先明确范围、不做内容、影响面、回归风险和验证方式。
- 给出计划或方案前必须显式判断是否过度设计：确认目标是否真的需要新增脚本、抽象、自动化、编辑器工具、常驻系统、运行时控制器或测试框架；能用场景 / Prefab / 配置 / 现有工具直接交付的，不默认升级为程序化控制或新系统。方案中应说明选择当前复杂度的理由，以及哪些内容刻意不做。
- Animator、输入、状态机属于高联动区域；修改时必须同时检查代码状态、Animator 条件、输入快照、相邻状态和 Play Mode 行为。
- 制定计划前必须先做语义审计：先按玩法语义分类，再考虑复用现有代码结构；现有实现相似不等于语义相同。
- 计划中提到复用、合并、优先级、叠加或冲突时，必须说明这是玩法语义冲突，还是底层资源共享导致的技术仲裁；不得把技术仲裁描述成玩法语义叠加。
- 复杂计划必须列出至少一个“不应触发 / 不应复用”的反例，用来验证边界；若反例推翻方案，先修正计划再提交。
- 用户中途修改计划时，先评估影响范围，再更新需求变更和相关设计文档，最后改代码。
- 最终回复必须说明修改了哪些文档、哪些代码、验证了什么、未处理风险是什么。
- 如果测试或 Unity 校验无法执行，必须说明原因和风险。

## 6. 用户需求纠偏

用户意见默认视为需求输入，但不一定是准确技术结论。遇到以下情况必须给出指导意见：

- 与项目核心目标冲突。
- 显著增加非战斗系统工作量。
- 破坏现有状态机、输入语义或数据边界。
- 与 Unity API、包版本、渲染管线或性能常识冲突。
- 造成难以维护的耦合、重复定义或过度抽象。

低风险偏好直接执行；中风险设计变更说明影响范围并给推荐方案；高风险或破坏性变更先提出替代方案和取舍。用户明确坚持时按其选择执行，并在长期记忆或需求变更中标注风险。

## 7. 代码设计偏好

- 优先追求清晰、直接、可读、可维护，不为了未来可能需求提前框架化。
- 新功能前先检查现有相似代码、数据结构和目录职责，优先复用或扩展现有实现。
- 不因微小差异新增字段、结构体、类或文件；字段语义相同的数据优先合并为同一个模型。
- 新增抽象必须解决真实重复、复杂度、生命周期、序列化边界或测试边界；禁止只转发、改名、包一层的薄封装。
- 公共层只放通用语义，不放单个状态、单个角色、单个功能的临时业务视图。
- 私有逻辑优先留在本状态、本组件、本模块内；只有稳定复用且语义一致时才提取 helper、基类或接口。
- 本项目使用面向对象编程，但多态按字段结构、校验规则、生命周期和行为差异建立，不按功能名字或枚举值建立子类。
- 相似功能必须保持相似代码结构、命名和数据流；功能能跑只是最低要求，阅读一致性同样重要。
- 数据来源必须单一。不要为方便访问复制镜像字段；必须缓存时说明来源、刷新时机和失效规则。
- 当前 Demo 不优先保留长期兼容层。旧逻辑确认无价值后应删除，不长期保留多条并行路径。
- 缺关键配置或资产时应明确报错并拒绝动作或退回安全状态，不用硬编码 fallback 模拟正常玩法。
- 默认不主动新增 Debug 字段、Debug 面板、Debug 遥测快照、常驻调试 UI 或调试日志；只有用户明确要求增加 Debug 能力，或为定位当前 bug 所必需且交付后会移除/收敛时，才允许添加，并在交付中说明原因。

## 8. 编码、文档与验证

- 中文文件、中文注释、Inspector 文本统一使用 UTF-8。
- PowerShell 读取中文 Markdown 或中文注释必须显式 `-Encoding UTF8`；搜索中文文本必须 `Select-String -Encoding UTF8`。
- `$OutputEncoding` 不能替代 `Get-Content -Encoding UTF8`。
- 文档写入首选 `apply_patch`；如必须用 PowerShell 写文件，使用 UTF-8，必要时用 .NET UTF-8 无 BOM。
- 代码命名统一，避免重复定义；遵循 Unity C# 规则和项目既有风格。
- 注释重点说明数据字段、关键类、关键函数、设计约束和非显而易见逻辑。
- C# 函数 XML 注释规范：
  - `<summary>` 必须说明函数的具体职责、调用时机或关键副作用，禁止只复述函数名，禁止使用“收敛、消费、相关运行时、组件协作、维护一致性”等空泛描述。
  - 有参数的方法必须为每个参数写 `<param>`，说明参数在本函数中的语义、单位或输入 / 输出作用；`out` / `ref` 参数必须说明写回内容。
  - 非 `void` 方法必须写 `<returns>`，说明返回值含义、成功 / 失败条件或单位；返回 `bool` 时要写清 `true` / `false` 分别代表什么。
  - `<remarks>` 只用于补充调用顺序、状态机边界、性能注意事项或设计原因；不要把正文堆进 `<summary>`。
  - 私有 helper 也遵守上述规则；只有无参数、无返回值且行为直观的 Unity 生命周期方法可只写一句明确的 `<summary>`。
  - 注释必须贴近当前实现，修改逻辑、参数语义或返回值时同步更新注释；不要保留过时描述。
- 优先小步修改，每步尽量保持可编译、可验证；不要擅自回滚用户已有修改。
- 需求文档必须与代码语义一致；长期记忆和需求变更记录必须简明、可恢复上下文。
- 代码变更后优先运行 Unity 脚本校验或相关 Unity Test Framework 测试。
- 验证不能只覆盖当前 bug；涉及状态机或动画时必须覆盖相邻状态、取消窗口、动画回路、Debug 输出和文档一致性。
- 文档变更至少检查 UTF-8、关键章节和路径是否正确。
- Console error 必须读取并区分项目错误与工具通信错误。

## 9. 工具策略

- 默认工具优先级：`rg` / 本地文件、Unity MCP、官方文档、必要时 web 搜索。
- 复杂推理使用 `sequential-thinking`。
- 当前内置工具、Unity MCP、官方文档工具、web 搜索和 `.codex/skills` 足够支撑当前阶段。
- 只有代码检索成本过高或多代理任务管理成为瓶颈时，再考虑新增 MCP；不要把未配置工具写成硬依赖。
